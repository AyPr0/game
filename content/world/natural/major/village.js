(function () {
    //just a village; markets,jobs,housing,bedding
    switch(tmp){
        case x:
            t=`You enter the village.`
            n=`\${btn('leave',"tmpn=10;tmp2='out';next(w+'terrain/${chunk[loc()].arr(0)}')")}`
            break;
    }
    end()
})()
(function () {
    //the base of a large organization. high value trading,high value jobs,bedding
    switch(tmp){
        case x:
            t=`You enter the fort. People walk in and out of the various buildings.`
            n=`\${btn('leave',"tmpn=10;tmp2='out';next(w+'terrain/${chunk[loc()].arr(0)}')")}`
            break;
    }
    end()
})()